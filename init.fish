function init
  abbr -a z              sudo zypper
  abbr -a zh             sudo zypper -h
  abbr -a zhse           sudo zypper -h se
  abbr -a zlicenses      sudo zypper licenses
  abbr -a zps            sudo zypper ps
  abbr -a zshell         sudo zypper shell
  abbr -a zsource        sudo zypper source-download
  abbr -a ztos           sudo zypper tos
  abbr -a zvcmp          sudo zypper vcmp
  abbr -a zin            sudo zypper in
  abbr -a zinr           sudo zypper in r
  abbr -a zrm            sudo zypper rm
  abbr -a zsi            sudo zypper si
  abbr -a zin            sudo zypper in
  abbr -a zinr           sudo zypper inr
  abbr -a zrm            sudo zypper rm
  abbr -a zsi            sudo zypper si
  abbr -a zve            sudo zypper ve
  abbr -a zdup           sudo zypper dup
  abbr -a zlp            sudo zypper lp
  abbr -a zlu            sudo zypper lu
  abbr -a zpchk          sudo zypper pchk
  abbr -a zup            sudo zypper up
  abbr -a zpatch         sudo zypper patch
  abbr -a zif            sudo zypper if
  abbr -a zpa            sudo zypper pa
  abbr -a zpatch-info    sudo zypper patch-info
  abbr -a zpattern-info  sudo zypper pattern-info
  abbr -a zproduct-info  sudo zypper product-info
  abbr -a zpch           sudo zypper pch
  abbr -a zpd            sudo zypper pd
  abbr -a zpt            sudo zypper pt
  abbr -a zse            sudo zypper se
  abbr -a zwp            sudo zypper wp
  abbr -a zar            sudo zypper ar
  abbr -a zcl            sudo zypper clean
  abbr -a zlr            sudo zypper lr
  abbr -a zmr            sudo zypper mr
  abbr -a znr            sudo zypper nr
  abbr -a zref           sudo zypper ref
  abbr -a zrr            sudo zypper rr
  abbr -a zas            sudo zypper as
  abbr -a zms            sudo zypper ms
  abbr -a zrefs          sudo zypper refs
  abbr -a zrs            sudo zypper rs
  abbr -a zls            sudo zypper ls
  abbr -a zal            sudo zypper al
  abbr -a zcl            sudo zypper cl
  abbr -a zll            sudo zypper ll
  abbr -a zrl            sudo zypper rl
end

